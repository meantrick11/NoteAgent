# n10

## 1. 题头

- seq: 10
- id: `n10`
- kind: `quality`
- style: `faithful_paragraphs`
- rubric_version: `v0.1`
- dataset: `evals/prompt/cases.jsonl`
- filter: `all`
- label: `rag-v10`

## 2. 得分摘要

- 行为门: 通过
- 总分: 72.28
- 忠实: 50.0
- 完整: 100.0
- 结构: 55.0
- 流畅: 不适用（摊权）
- 形态: 78.12
- 可检索: 100.0

## 3. 小指标

| id | 名称 | 档位 | 权重 | 适用 | 证据 |
|----|------|------|------|------|------|
| `faithful.contradiction` | 矛盾/编造 | — | 30 | 否 | 本期无 Judge，不评矛盾 |
| `faithful.literals` | 字面量/标识符 | 10 | 20 | 是 | 草稿字面量均可在材料中找到 |
| `faithful.hedge` | 情态 | 0 | 20 | 是 | 情态被加强: 必须 |
| `faithful.unsupported` | 无支持命题 | — | 20 | 否 | 本期无 Judge，不评无支持命题 |
| `faithful.inference` | 推论标注 | — | 10 | 否 | 本期无 Judge，不评推论标注 |
| `complete.anchors` | 锚点覆盖 | 10 | 20 | 是 | 全部命中 |
| `complete.terms` | 术语覆盖 | 10 | 15 | 是 | 术语覆盖 22/22 |
| `complete.commands` | 命令覆盖 | 10 | 15 | 是 | 全部命中 |
| `complete.examples` | 例子覆盖 | 10 | 15 | 是 | 全部命中 |
| `complete.compression` | 节篇幅/压缩 | 10 | 25 | 是 | 压缩比 1.11，体量与任务匹配 |
| `complete.scope` | 范围 | 10 | 10 | 是 | 范围与任务一致 |
| `structure.heading_recall` | 标题原文 | 10 | 30 | 是 | 原标题含编号全在 |
| `structure.heading_precision` | 标题无发明 | 5 | 30 | 是 | 额外标题: -*- coding: encoding -*- |
| `structure.heading_level` | 层级映射 | 0 | 20 | 是 | 2.1.1. 传入参数: 期望 h4 实际 h3；2.1.2. 交互模式: 期望 h4 实际 h3 |
| `structure.h1` | 正文 H1 | 0 | 10 | 是 | create/append 写了 #  |
| `structure.merge` | 未合并 | 10 | 10 | 是 | 未合并 |
| `fluent.readability` | 通顺 | — | 50 | 否 | 本期无 Judge，不评通顺 |
| `fluent.consistency` | 术语一致 | — | 30 | 否 | 本期无 Judge，不评术语一致 |
| `fluent.translation` | 译文通顺 | — | 20 | 否 | 非翻译任务 |
| `form.slogan` | 非口号体 | 5 | 25 | 是 | 1 节像口号 |
| `form.fence` | 代码围栏 | 10 | 25 | 是 | 草稿含围栏 |
| `form.quote` | 备注引用 | — | 20 | 否 | 材料无备注 |
| `form.list` | 列表是否符合任务 | 10 | 15 | 是 | 列表行占比 0.16 |
| `form.indent` | 禁止只靠缩进当代码 | 10 | 10 | 是 | 未用缩进冒充围栏 |
| `form.spacing` | 块间空行 | 0 | 5 | 是 | 6 处块间缺空行 |
| `retrievable.file_name_ok` | 路径合法 | 10 | 60 | 是 | 路径合法 |
| `retrievable.file_name_topic` | 文件名能表达主题 | 10 | 40 | 是 | stem 能看出主题 |

## 4. 行为

- expect_propose: `True`
- actual_propose: `True`
- expect_tools_prefix: `['list_files']`
- actual_tools: `['list_files', 'propose_note']`
- expect_action: `None`
- actual_action: `create`
- 行为门通过

## 5. 输入

```text
请把下面整理成笔记记下来。

2. 使用 Python 的解释器
本节说明如何启动解释器、如何传入参数，以及源文件默认用什么编码。下面各小节应保留原文结构，不要合并成一条「用法总结」。
2.1. 唤出解释器
在可用的机器上 Python 解释器通常被安装为 /usr/local/bin/python3.14；将 /usr/local/bin 加入 Unix shell 搜索路径后键入 python3.14 即可启动。解释器也可能安装在别的位置，不要把「通常」写成「一定在这个路径」。在 Windows 上从 Microsoft Store 安装后 python3.14 可用；若安装了 py.exe 启动器则可用 py。主提示符下 Unix 用 Control-D、Windows 用 Control-Z 退出，退出状态码为 0；也可输入 quit()。另一种启动方式是 python -c command [arg] ...。模块可当作脚本：python -m module [arg] ...。由于语句里常有空格，通常建议用引号把 command 括起来。
2.1.1. 传入参数
解释器把脚本名与其他参数转化为字符串列表存到 sys 模块的 argv。执行 import sys 可访问。未给定输入参数时 sys.argv[0] 是空字符串。使用 -c command 时 sys.argv[0] 是 -c。使用 -m module 时 sys.argv[0] 是包含目录的模块全名。这些规则对交互启动同样成立，不要发明其它下标含义。
2.1.2. 交互模式
在终端输入并执行指令时，解释器在交互模式中运行。主提示符通常为 >>>，次要提示符为 ...。多行语句在次要提示符下继续，直到语法完整。
2.2. 解释器的运行环境
运行环境还包括源文件如何被解码。下面只讲编码，不要扩写成虚拟机或路径配置。
2.2.1. 源文件的字符编码
默认情况下 Python 源码文件的编码是 UTF-8。不使用默认编码时，文件第一行写成 # -*- coding: encoding -*-。声明必须出现在第一或第二行，不要改成任意注释位置。
```

## 6. 工具轨迹

### 1. `list_files`

- status: `ok`
- args: {}
- output: {"files": [], "folders": []}

### 2. `propose_note`

- status: `ok`
- args: {"action": "create", "file_name": "Python解释器使用.md", "content": "## 2. 使用 Python 的解释器\n\n本节说明如何启动解释器、如何传入参数，以及源文件默认使用什么编码；下面各小节保留原文结构。\n\n### 2.1. 唤出解释器\n\n在可用的机器上，Python 解释器通常被安装为 `/usr/local/bin/python3.14`；把 `/usr/local/bin` 加入 Unix shell…
- output: {"status": "pending_review", "action": "create", "file_name": "Python解释器使用.md"}

## 7. 草稿

- action: `create`
- file_name: `Python解释器使用.md`
- reason: 用户要求记下并保留原文各小节结构，notes/ 下暂无笔记，故新建一份 Python 解释器使用笔记，按 2 / 2.1 / 2.1.1 等编号建立层级。

```markdown
## 2. 使用 Python 的解释器

本节说明如何启动解释器、如何传入参数，以及源文件默认使用什么编码；下面各小节保留原文结构。

### 2.1. 唤出解释器

在可用的机器上，Python 解释器通常被安装为 `/usr/local/bin/python3.14`；把 `/usr/local/bin` 加入 Unix shell 搜索路径后，键入 `python3.14` 即可启动。解释器也可能安装在别的位置。

在 Windows 上从 Microsoft Store 安装后 `python3.14` 可用；若安装了 `py.exe` 启动器，则可用 `py`。

在主提示符下退出：Unix 用 `Control-D`，Windows 用 `Control-Z`，退出状态码为 0；也可以输入 `quit()`。

另一种启动方式是把命令作为字符串传入：

```bash
python -c command [arg] ...
```

模块可以当作脚本运行：

```bash
python -m module [arg] ...
```

由于语句中常有空格，通常建议用引号把 `command` 括起来。

### 2.1.1. 传入参数

解释器把脚本名与其他参数转化为字符串列表，存放在 `sys` 模块的 `argv` 中；执行 `import sys` 即可访问。

- 未给定输入参数时，`sys.argv[0]` 是空字符串。
- 使用 `-c command` 时，`sys.argv[0]` 是 `-c`。
- 使用 `-m module` 时，`sys.argv[0]` 是包含目录的模块全名。

这些规则对交互启动同样成立。

### 2.1.2. 交互模式

在终端输入并执行指令时，解释器在交互模式中运行。主提示符通常为 `>>>`，次要提示符为 `...`；多行语句在次要提示符下继续，直到语法完整。

### 2.2. 解释器的运行环境

运行环境还包括源文件如何被解码；本小节只讲编码。

#### 2.2.1. 源文件的字符编码

默认情况下，Python 源码文件的编码是 UTF-8。

不使用默认编码时，在文件第一行写成：

```text
# -*- coding: encoding -*-
```

该声明必须出现在第一行或第二行。
```

## 8. 助手气泡

已按原文编号结构提交新建 `Python解释器使用.md` 的草稿，等你在卡片上审批。
