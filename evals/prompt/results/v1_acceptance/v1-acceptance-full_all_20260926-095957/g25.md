# g25

## 1. 题头

- seq: 25
- id: `g25`
- kind: `quality`
- style: `faithful_paragraphs`
- rubric_version: `v0.1`
- dataset: `evals/prompt/v1_acceptance.jsonl`
- filter: `all`
- label: `v1-acceptance-full`

## 2. 得分摘要

- 行为门: 通过
- 总分: 93.76
- 忠实: 100.0
- 完整: 100.0
- 结构: 81.25
- 流畅: 不适用（摊权）
- 形态: 96.88
- 可检索: 80.0

## 3. 小指标

| id | 名称 | 档位 | 权重 | 适用 | 证据 |
|----|------|------|------|------|------|
| `faithful.contradiction` | 矛盾/编造 | — | 30 | 否 | 本期无 Judge，不评矛盾 |
| `faithful.literals` | 字面量/标识符 | 10 | 20 | 是 | 材料无明显字面量，草稿也未发明 |
| `faithful.hedge` | 情态 | — | 20 | 否 | 材料无情态词 |
| `faithful.unsupported` | 无支持命题 | — | 20 | 否 | 本期无 Judge，不评无支持命题 |
| `faithful.inference` | 推论标注 | — | 10 | 否 | 本期无 Judge，不评推论标注 |
| `complete.anchors` | 锚点覆盖 | 10 | 20 | 是 | 全部命中 |
| `complete.terms` | 术语覆盖 | 10 | 15 | 是 | 术语覆盖 5/5 |
| `complete.commands` | 命令覆盖 | — | 15 | 否 | 材料无命令 |
| `complete.examples` | 例子覆盖 | — | 15 | 否 | 材料无例子 |
| `complete.compression` | 节篇幅/压缩 | 10 | 25 | 是 | 压缩比 1.96，体量与任务匹配 |
| `complete.scope` | 范围 | 10 | 10 | 是 | 范围与任务一致 |
| `structure.heading_recall` | 标题原文 | 10 | 30 | 是 | 原标题含编号全在 |
| `structure.heading_precision` | 标题无发明 | 5 | 30 | 是 | 额外标题: 工具函数 |
| `structure.heading_level` | 层级映射 | — | 20 | 否 | 材料无编号标题 |
| `structure.h1` | 正文 H1 | 10 | 10 | 是 | replace 保留一级标题 |
| `structure.merge` | 未合并 | 10 | 10 | 是 | 无需判断合并 |
| `fluent.readability` | 通顺 | — | 50 | 否 | 本期无 Judge，不评通顺 |
| `fluent.consistency` | 术语一致 | — | 30 | 否 | 本期无 Judge，不评术语一致 |
| `fluent.translation` | 译文通顺 | — | 20 | 否 | 非翻译任务 |
| `form.slogan` | 非口号体 | 10 | 25 | 是 | 默认以段落为主 |
| `form.fence` | 代码围栏 | 10 | 25 | 是 | 草稿含围栏 |
| `form.quote` | 备注引用 | — | 20 | 否 | 材料无备注 |
| `form.list` | 列表是否符合任务 | 10 | 15 | 是 | 列表行占比 0.00 |
| `form.indent` | 禁止只靠缩进当代码 | 10 | 10 | 是 | 未用缩进冒充围栏 |
| `form.spacing` | 块间空行 | 5 | 5 | 是 | 2 处缺空行 |
| `retrievable.file_name_ok` | 路径合法 | 10 | 60 | 是 | 路径合法 |
| `retrievable.file_name_topic` | 文件名能表达主题 | 5 | 40 | 是 | 文件名勉强相关 |

## 4. 行为

- expect_propose: `True`
- actual_propose: `True`
- expect_tools_prefix: `['list_files', 'read_file']`
- actual_tools: `['list_files', 'read_file', 'propose_note']`
- expect_action: `replace`
- actual_action: `replace`
- 行为门通过

## 5. 输入

```text
Python/工具函数.md 里那个重试函数的签名要改一下，改成可以指定退避上限的版本，签名换成下面这个，缓存那一节和它的代码都不要动，路径也保持不变。

```python
def retry(times, delay, max_delay=None):
    ...
```

加上 max_delay 之后，等待时间在超过上限时按上限处理；不传这个参数时行为和原来一样，这样调用方不用全部改一遍。
```

seed_files:

### Python/工具函数.md

```markdown
# 工具函数

## 重试

```python
def retry(times, delay):
    for _ in range(times):
        ...
```

## 缓存

```python
@lru_cache(maxsize=32)
def load(name):
    ...
```
```

## 6. 工具轨迹

### 1. `list_files`

- status: `ok`
- args: {}
- output: {"files": ["Python/工具函数.md"], "folders": ["Python"]}

### 2. `read_file`

- status: `ok`
- args: {"file_name": "Python/工具函数.md"}
- output: {"file_content": "# 工具函数\n\n## 重试\n\n```python\ndef retry(times, delay):\n for _ in range(times):\n ...\n```\n\n## 缓存\n\n```python\n@lru_cache(maxsize=32)\ndef load(name):\n ...\n```\n", "source_id": 1}

### 3. `propose_note`

- status: `ok`
- args: {"action": "replace", "file_name": "Python/工具函数.md", "content": "# 工具函数\n\n## 重试\n\n```python\ndef retry(times, delay, max_delay=None):\n for _ in range(times):\n ...\n```\n\n新增 `max_delay` 用来指定退避上限：等待时间超过上限时按上限处理；不传该参数时行为与原来一致，调用方不必全部改一遍。\…
- output: {"status": "pending_review", "action": "replace", "file_name": "Python/工具函数.md"}

## 7. 草稿

- action: `replace`
- file_name: `Python/工具函数.md`
- reason: 用户要求更正已有笔记中重试函数的签名并增加退避上限说明，属覆盖更新，保留原有缓存一节。

```markdown
# 工具函数

## 重试

```python
def retry(times, delay, max_delay=None):
    for _ in range(times):
        ...
```

新增 `max_delay` 用来指定退避上限：等待时间超过上限时按上限处理；不传该参数时行为与原来一致，调用方不必全部改一遍。

## 缓存

```python
@lru_cache(maxsize=32)
def load(name):
    ...
```
```

## 8. 助手气泡

草稿已提交审批（覆盖 `Python/工具函数.md`）：`retry` 签名改为 `retry(times, delay, max_delay=None)`，并说明超过上限按上限处理、不传时与原行为一致；「缓存」一节及其代码未改动。

注意：我没有写入磁盘，等你点同意后由系统写入。
