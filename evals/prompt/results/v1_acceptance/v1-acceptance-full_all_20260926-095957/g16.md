# g16

## 1. 题头

- seq: 16
- id: `g16`
- kind: `quality`
- style: `faithful_paragraphs`
- rubric_version: `v0.1`
- dataset: `evals/prompt/v1_acceptance.jsonl`
- filter: `all`
- label: `v1-acceptance-full`

## 2. 得分摘要

- 行为门: 通过
- 总分: 82.28
- 忠实: 100.0
- 完整: 100.0
- 结构: 40.0
- 流畅: 不适用（摊权）
- 形态: 93.75
- 可检索: 80.0

## 3. 小指标

| id | 名称 | 档位 | 权重 | 适用 | 证据 |
|----|------|------|------|------|------|
| `faithful.contradiction` | 矛盾/编造 | — | 30 | 否 | 本期无 Judge，不评矛盾 |
| `faithful.literals` | 字面量/标识符 | 10 | 20 | 是 | 材料无明显字面量，草稿也未发明 |
| `faithful.hedge` | 情态 | 10 | 20 | 是 | 情态保留 |
| `faithful.unsupported` | 无支持命题 | — | 20 | 否 | 本期无 Judge，不评无支持命题 |
| `faithful.inference` | 推论标注 | — | 10 | 否 | 本期无 Judge，不评推论标注 |
| `complete.anchors` | 锚点覆盖 | 10 | 20 | 是 | 全部命中 |
| `complete.terms` | 术语覆盖 | 10 | 15 | 是 | 术语覆盖 9/9 |
| `complete.commands` | 命令覆盖 | — | 15 | 否 | 材料无命令 |
| `complete.examples` | 例子覆盖 | 10 | 15 | 是 | 全部命中 |
| `complete.compression` | 节篇幅/压缩 | 10 | 25 | 是 | 压缩比 1.16，体量与任务匹配 |
| `complete.scope` | 范围 | 10 | 10 | 是 | 范围与任务一致 |
| `structure.heading_recall` | 标题原文 | — | 30 | 否 | 材料无原标题 |
| `structure.heading_precision` | 标题无发明 | 0 | 30 | 是 | 额外标题: 指数退避等待时间, 参数与返回值, 调用示例, 设计约定 |
| `structure.heading_level` | 层级映射 | — | 20 | 否 | 材料无编号标题 |
| `structure.h1` | 正文 H1 | 10 | 10 | 是 | 正文未写违规一级标题 |
| `structure.merge` | 未合并 | 10 | 10 | 是 | 无需判断合并 |
| `fluent.readability` | 通顺 | — | 50 | 否 | 本期无 Judge，不评通顺 |
| `fluent.consistency` | 术语一致 | — | 30 | 否 | 本期无 Judge，不评术语一致 |
| `fluent.translation` | 译文通顺 | — | 20 | 否 | 非翻译任务 |
| `form.slogan` | 非口号体 | 10 | 25 | 是 | 默认以段落为主 |
| `form.fence` | 代码围栏 | 10 | 25 | 是 | 草稿含围栏 |
| `form.quote` | 备注引用 | — | 20 | 否 | 材料无备注 |
| `form.list` | 列表是否符合任务 | 10 | 15 | 是 | 列表行占比 0.25 |
| `form.indent` | 禁止只靠缩进当代码 | 10 | 10 | 是 | 未用缩进冒充围栏 |
| `form.spacing` | 块间空行 | 0 | 5 | 是 | 5 处块间缺空行 |
| `retrievable.file_name_ok` | 路径合法 | 10 | 60 | 是 | 路径合法 |
| `retrievable.file_name_topic` | 文件名能表达主题 | 5 | 40 | 是 | 文件名勉强相关 |

## 4. 行为

- expect_propose: `True`
- actual_propose: `True`
- expect_tools_prefix: `['list_files']`
- actual_tools: `['list_files', 'search_relative_from_chromadb', 'propose_note']`
- expect_action: `create`
- actual_action: `create`
- 行为门通过

## 5. 输入

```text
请把下面这段 Python 代码和说明整理成笔记记下来，函数、输入输出例子、返回值和缩进都要正确，代码要用围栏。

下面这个函数用来算指数退避的等待时间。

```python
def retry_delay(attempt, base=0.5, max_delay=30):
    delay = base * (2 ** attempt)
    return min(delay, max_delay)
```

attempt 从 0 开始计数，所以第一次等待是 base 本身。base 的默认值是 0.5 秒，max_delay 的默认值是 30 秒。函数返回的是秒数，返回类型是浮点数，调用方可以直接把它交给 sleep。

调用例子：

```python
>>> retry_delay(0)
0.5
>>> retry_delay(3)
4.0
>>> retry_delay(10)
30
```

注意第三次调用的返回值被 max_delay 截断成了 30，所以重试次数再多，等待时间也不会超过上限。如果想让等待时间随机化，可以在返回值上乘一个随机系数，但这一步不要放进这个函数里，保持它只做纯计算。
```

## 6. 工具轨迹

### 1. `list_files`

- status: `ok`
- args: {}
- output: {"files": [], "folders": []}

### 2. `search_relative_from_chromadb`

- status: `ok`
- args: {"query": "指数退避 重试等待时间 retry delay"}
- output: {"fragments": [], "count": 0}

### 3. `propose_note`

- status: `ok`
- args: {"action": "create", "file_name": "ExponentialBackoff.md", "reason": "没有相近已有笔记，按用户材料新建记录该函数的代码、参数、返回值和调用示例。", "similar": "", "content": "## 指数退避等待时间\n\n下面这个函数用来算指数退避的等待时间：\n\n```python\ndef retry_delay(attempt, base=0.5, max_delay=30):\n de…
- output: {"status": "pending_review", "action": "create", "file_name": "ExponentialBackoff.md"}

## 7. 草稿

- action: `create`
- file_name: `ExponentialBackoff.md`
- reason: 没有相近已有笔记，按用户材料新建记录该函数的代码、参数、返回值和调用示例。

```markdown
## 指数退避等待时间

下面这个函数用来算指数退避的等待时间：

```python
def retry_delay(attempt, base=0.5, max_delay=30):
    delay = base * (2 ** attempt)
    return min(delay, max_delay)
```

### 参数与返回值

- `attempt`：从 0 开始计数，所以第一次等待就是 `base` 本身。
- `base`：默认值是 0.5 秒。
- `max_delay`：默认值是 30 秒，作为等待时间的上限。
- 函数返回的是秒数，返回类型是浮点数，调用方可以直接把它交给 `sleep`。

### 调用示例

```python
>>> retry_delay(0)
0.5
>>> retry_delay(3)
4.0
>>> retry_delay(10)
30
```

> 第三次调用的返回值被 `max_delay` 截断成了 30，所以重试次数再多，等待时间也不会超过上限。

### 设计约定

如果想让等待时间随机化，可以在返回值上乘一个随机系数，但这一步不要放进这个函数里，保持它只做纯计算。
```

## 8. 助手气泡

已提交新建 `ExponentialBackoff.md` 的审批，代码用 python 围栏、保留了缩进与输入输出示例，并单列了参数、返回值与「随机化不放进函数」的设计约定。请确认后由系统写入。
