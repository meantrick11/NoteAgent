# g21

## 1. 题头

- seq: 03
- id: `g21`
- kind: `quality`
- style: `faithful_paragraphs`
- rubric_version: `v0.1`
- dataset: `evals/prompt/v1_acceptance.jsonl`
- filter: `g01,g16,g21`
- label: `v1-acceptance-smoke`

## 2. 得分摘要

- 行为门: 通过
- 总分: 99.13
- 忠实: 100.0
- 完整: 100.0
- 结构: 100.0
- 流畅: 不适用（摊权）
- 形态: 100.0
- 可检索: 80.0

## 3. 小指标

| id | 名称 | 档位 | 权重 | 适用 | 证据 |
|----|------|------|------|------|------|
| `faithful.contradiction` | 矛盾/编造 | — | 30 | 否 | 本期无 Judge，不评矛盾 |
| `faithful.literals` | 字面量/标识符 | 10 | 20 | 是 | 材料无明显字面量，草稿也未发明 |
| `faithful.hedge` | 情态 | 10 | 20 | 是 | 情态保留 |
| `faithful.unsupported` | 无支持命题 | — | 20 | 否 | 本期无 Judge，不评无支持命题 |
| `faithful.inference` | 推论标注 | — | 10 | 否 | 本期无 Judge，不评推论标注 |
| `complete.anchors` | 锚点覆盖 | 10 | 20 | 是 | 全部命中 |
| `complete.terms` | 术语覆盖 | 10 | 15 | 是 | 术语覆盖 5/5 |
| `complete.commands` | 命令覆盖 | — | 15 | 否 | 材料无命令 |
| `complete.examples` | 例子覆盖 | — | 15 | 否 | 材料无例子 |
| `complete.compression` | 节篇幅/压缩 | 10 | 25 | 是 | 压缩比 1.05，体量与任务匹配 |
| `complete.scope` | 范围 | 10 | 10 | 是 | 范围与任务一致 |
| `structure.heading_recall` | 标题原文 | 10 | 30 | 是 | 原标题含编号全在 |
| `structure.heading_precision` | 标题无发明 | 10 | 30 | 是 | 无发明标题 |
| `structure.heading_level` | 层级映射 | — | 20 | 否 | 材料无编号标题 |
| `structure.h1` | 正文 H1 | 10 | 10 | 是 | 正文未写违规一级标题 |
| `structure.merge` | 未合并 | 10 | 10 | 是 | 无需判断合并 |
| `fluent.readability` | 通顺 | — | 50 | 否 | 本期无 Judge，不评通顺 |
| `fluent.consistency` | 术语一致 | — | 30 | 否 | 本期无 Judge，不评术语一致 |
| `fluent.translation` | 译文通顺 | — | 20 | 否 | 非翻译任务 |
| `form.slogan` | 非口号体 | 10 | 25 | 是 | 默认以段落为主 |
| `form.fence` | 代码围栏 | — | 25 | 否 | 材料无代码态 |
| `form.quote` | 备注引用 | 10 | 20 | 是 | 备注落到 > |
| `form.list` | 列表是否符合任务 | 10 | 15 | 是 | 列表行占比 0.00 |
| `form.indent` | 禁止只靠缩进当代码 | 10 | 10 | 是 | 未用缩进冒充围栏 |
| `form.spacing` | 块间空行 | 10 | 5 | 是 | 块上下空一行 |
| `retrievable.file_name_ok` | 路径合法 | 10 | 60 | 是 | 路径合法 |
| `retrievable.file_name_topic` | 文件名能表达主题 | 5 | 40 | 是 | 文件名勉强相关 |

## 4. 行为

- expect_propose: `True`
- actual_propose: `True`
- expect_tools_prefix: `['list_files']`
- actual_tools: `['list_files', 'read_file', 'propose_note']`
- expect_action: `append`
- actual_action: `append`
- 行为门通过

## 5. 输入

```text
把我下面这段补到 Go.md 末尾，作为新的一节记下来，原来那节不要动。

## 切片

切片是对底层数组的视图，长度用 len 取，容量用 cap 取；追加用 append，超出容量时会重新分配底层数组并拷贝。`s := make([]int, 0, 4)` 表示长度为 0、容量为 4 的切片，这时追加 4 个元素都不会重新分配。

备注：切片共享底层数组，修改一个切片的元素可能影响另一个，只有在 append 触发扩容之后才会分开。
```

seed_files:

### Go.md

```markdown
# Go

## 控制流

Go 只有 `for` 一种循环，`if` 可以带初始化语句。switch 默认每个分支自动 break。
```

## 6. 工具轨迹

### 1. `list_files`

- status: `ok`
- args: {}
- output: {"files": ["Go.md"], "folders": []}

### 2. `read_file`

- status: `ok`
- args: {"file_name": "Go.md"}
- output: {"file_content": "# Go\n\n## 控制流\n\nGo 只有 `for` 一种循环，`if` 可以带初始化语句。switch 默认每个分支自动 break。\n", "source_id": 1}

### 3. `propose_note`

- status: `ok`
- args: {"action": "append", "file_name": "Go.md", "reason": "用户要求把切片一节补到已有 Go.md 末尾，主题吻合且原文无此内容，故追加新节不动原节。", "similar": "Go.md", "content": "## 切片\n\n切片是对底层数组的视图，长度用 `len` 取，容量用 `cap` 取；追加用 `append`，超出容量时会重新分配底层数组并拷贝。`s := make([]int, 0, 4)` 表示长度为…
- output: {"status": "pending_review", "action": "append", "file_name": "Go.md"}

## 7. 草稿

- action: `append`
- file_name: `Go.md`
- reason: 用户要求把切片一节补到已有 Go.md 末尾，主题吻合且原文无此内容，故追加新节不动原节。

```markdown
## 切片

切片是对底层数组的视图，长度用 `len` 取，容量用 `cap` 取；追加用 `append`，超出容量时会重新分配底层数组并拷贝。`s := make([]int, 0, 4)` 表示长度为 0、容量为 4 的切片，这时追加 4 个元素都不会重新分配。

> 备注：切片共享底层数组，修改一个切片的元素可能影响另一个，只有在 `append` 触发扩容之后才会分开。
```

## 8. 助手气泡

已提交草稿：在 `Go.md` 末尾追加「切片」一节，原有的「控制流」一节保持不变。请审批。
