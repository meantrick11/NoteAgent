# decisions

架构决策记录（ADR）。只追加，不改写已接受决策的历史正文。现阶段可以没有 ADR 文件。

## 包含模块

暂无 `NNNN-title.md`。

## 基础使用

新增决策时在本目录添加：

```text
0001-hitl-before-write.md
```

文内写清：状态（proposed / accepted / superseded）、上下文、决策、后果。已被取代的 ADR 把状态改为 superseded，并链接新文档，不要删原文。
